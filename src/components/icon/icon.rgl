<template>
	<i class="r-icon iconfont { spin ? 'r-icon-spin' : '' }">{#inc this.$body}</i>
</template>

<script>
	import '../../fonts/iconfont.css';

	export default {
		name: 'Icon'
	}
</script>

<style lang="less" scoped>
	.r-icon {
		display: inline-block;
		min-width: 1em;
	}

	.r-icon-spin {
		animation: iconspin .8s linear infinite;
	}

	@keyframes iconspin {
		0% {
			transform: rotate(0);
		}
		100% {
			transform: rotate(360deg);
		}
	}
</style>
