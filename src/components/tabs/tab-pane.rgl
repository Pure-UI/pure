<template>
	{#if show}
		{#inc this.$body}
	{/if}
</template>

<script>
	export default {
		name: 'TabPane',
		config() {
			this.data.show = false;

			if ( this.$outer && this.$outer.ping ) {
				this.$outer.ping( this );
			}
		}
	};
</script>

<style lang="less" scoped>

</style>
