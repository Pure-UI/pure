<template>
	<span class="pure-icon { spin ? 'pure-icon--rotate' : '' }" style="{ opacity ? 'opacity: ' + opacity + ';' : '' }{ size ? 'font-size: ' + size + ';' : '' }">{#inc this.$body}</span>
</template>

<script>
	export default {};
</script>
