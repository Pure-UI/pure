<template>
	<div class="pure-form-label">
		{#inc this.$body}
	</div>
</template>

<script>
	export default {
		init() {
			if ( this.$outer && this.$outer.ping ) {
				this.$outer.ping( this );
			}
		}
	};
</script>
